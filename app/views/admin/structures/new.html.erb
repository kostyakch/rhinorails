<% provide(:title, t(:_NEW_PAGE)) %>

<%= form_for @page, :url => admin_structures_path, :multipart => true do |f| %>
    <legend><%= t(:_NEW_PAGE) %> </legend>   

   	<%= render 'fields', f: f %>	   
<% end %>

<%= render 'admin/page_fields/form', page: @page %>

<% content_for :javascript do %>
<script type="text/javascript">
<!--
	<%= raw(field_javascript) %>
	<%= raw(tab_javascript) %>
-->
</script>	
<% end %>
