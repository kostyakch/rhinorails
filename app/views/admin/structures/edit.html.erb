<% provide(:title, "#{t(:_EDIT)}: #{@page.name}") %>

<%= form_for @page, :url => admin_structure_path do |f| %>
    <legend><%= t(:_EDIT) %>: <%= @page.name %></legend>

<%= debug(@page.page_field) %>

   	<%= render 'fields', f: f %>	    
<% end %>

<% content_for :javascript do %>
<script type="text/javascript">
<!--
	<%= raw(field_javascript) %>
	<%= raw(tab_javascript) %>
-->
</script>	
<% end %>

