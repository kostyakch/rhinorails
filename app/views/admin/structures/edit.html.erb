<% provide(:title, "Редактирование: #{@page.name}") %>

<%= form_for @page, :url => admin_structure_path do |f| %>
    <legend>Редактирование раздела: <%= @page.name %></legend>

   	<%= render 'fields', f: f %>	    
<% end %>

<% content_for :javascript do %>
<script type="text/javascript">
<!--
	<%= raw(field_javascript) %>
	<%= raw(tab_javascript) %>
-->
</script>	
<% end %>

