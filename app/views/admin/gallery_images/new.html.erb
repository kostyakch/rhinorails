<% provide(:title, 'Добавление изображения') %>

<% render 'admin/galleries/sidebar' %>

<%= form_for @image, :url => admin_gallery_images_path, :html => {:multipart => true} do |f| %>
    <legend>Добавление изображения</legend>   

   	<%= render 'fields', f: f %>	   
<% end %>