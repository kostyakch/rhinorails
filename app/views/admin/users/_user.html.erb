<li>
  <%= gravatar_for user, size: 52 %>
  <%= link_to user.name, edit_admin_user_path(user) %>
  <% if has_role?('ROLE_ADMIN') && !current_user?(user) %>
    | <%= link_to "delete", admin_user_path(user), method: :delete, data: { confirm: t("You sure?") } %>
  <% end %>
</li>