<% provide(:title, 'Добавление параметра настройки') %>

<%= form_for @setting, :url => admin_settings_path do |c| %>
    <legend>Добавление раздела</legend>   

   	<%= render 'fields', f: c %>	   
<% end %>