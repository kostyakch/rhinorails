<h1><%= @page.field_by_name('h1') %></h1>
<% article_list(@page).each do |c| %>
<div>
	<p><%= link_to c.name, page_path(c.slug) %></p>

	<% if preview = c.content_by_name('preview') %>
		<p><%= preview.html_safe %></p>
	<% end %>
</div>	
<% end %>

